<h1>Listing workout_entries</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @workout_entries.each do |workout_entry| %>
  <tr>
    <td><%= link_to 'Show', workout_entry %></td>
    <td><%= link_to 'Edit', edit_workout_entry_path(workout_entry) %></td>
    <td><%= link_to 'Destroy', workout_entry, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Workout entry', new_workout_entry_path %>
